.modal.fade(id="artModal" tabindex="-1" role="dialog" aria-labelledby="artModal" aria-hidden="true")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(data-dismiss="modal")
          span &times;
      .modal-body
        img.art-large(src="{{digital.url}}")
        h3 {{digital.title}}
        p {{digital.medium}}
        p {{digital.notes}}
      .modal-footer
        button.btn.btn-primary(data-dismiss="modal") Cancel
